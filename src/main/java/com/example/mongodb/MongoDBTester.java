package com.example.mongodb;

public class MongoDBTester { 
   
	/*
   public static void main( String args[] ) {  
      
      // Creating a Mongo client 
      MongoClient mongo = new MongoClient("54.198.119.109", 27017); 
     
      // Creating Credentials 
      MongoCredential credential; 
      credential = MongoCredential.createCredential("app_mongo", "brwdev", "appUser123".toCharArray()); 
      
      // Accessing the database 
      MongoDatabase database = mongo.getDatabase("brwdev");

      // Retieving a collection
      MongoCollection<Document> collection = database.getCollection("google_place_search_and_photo"); 
      
      System.out.println("collection.find().first().get(\"name\"): "+ collection.toString());
      
      System.out.println("collection.find().first().get(\"name\"): "+ collection.estimatedDocumentCount());
   }
   */
}